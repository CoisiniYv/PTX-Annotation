# actions_mixin.py 说明文档

## 功能概述
`actions_mixin.py` 包含了应用程序的主要交互动作逻辑。它被设计为一个 Mixin 类，集成到主窗口中，用于处理用户触发的各种操作，如保存标注、加载文件、更新状态和快捷键响应。

## 主要逻辑与功能

*   **文件拖拽与加载:**
    *   `_on_mask_file_dropped(path)`: 处理用户将掩码文件（`.png`, `.nii`, `.nii.gz`）拖入画布的事件，并使用 SimpleITK/OpenCV 解析掩码。
    *   `load_single_pair()`: 允许用户直接选择一对 DICOM 和对应的掩码文件进行加载，进入“单对模式”。
    *   `load_quick_preview(image_path)`: 用于快速预览单张图像，自动在同级目录下寻找并匹配可能存在的掩码文件。
*   **掩码保存逻辑:**
    *   `save_mask()`: 核心保存函数。将画布上的掩码转换为二值图像，并根据原始加载的格式保存（支持导出为位图或 `NIfTI` 格式）。如果是 `NIfTI` 且原始存在，会保留其空间元数据。
    *   `_cleanup_empty_masks()`: 在关闭程序或切换病例时，自动清理那些全黑（没有任何标注）的冗余掩码文件。
*   **状态与标签更新:**
    *   `_update_pneumo_label(val)`: 响应快捷键（T/F），更新当前病例的“气胸阳性/阴性”状态。它不仅更新内部的 JSON 缓存，还会立即刷新左侧列表和下方缩略图（CT 模式）的 UI 颜色标记（红色/绿色边框）。
    *   `_update_status_ui()`: 刷新主窗口底部的状态栏信息，显示当前文件、目录、是否保存以及气胸状态。
*   **快捷导航:**
    *   `prev_image() / next_image()`: 序列内图像的上一张/下一张切换。
    *   `prev_case() / next_case()`: 列表间病例的上一例/下一例切换。
*   **导出与设置:**
    *   `export_current()`: 弹出导出设置对话框，允许用户将当前视图（包括图像和掩码）指定分辨率和反色状态导出。
    *   `open_perf_settings()`: 打开性能设置面板，动态调整预取数量和缓存大小。