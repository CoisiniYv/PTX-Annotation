# canvas.py 说明文档

## 功能概述
`canvas.py` 是应用程序的核心渲染和交互组件。它继承自 `QWidget`，负责在屏幕上绘制医学影像、叠加掩码，并处理所有与鼠标/键盘相关的绘图交互（如平移、缩放、笔刷涂抹）。

## 主要逻辑与功能

*   **多层级渲染架构:**
    *   **基础图层**: 支持显示 RGB 图像或从 16 位 DICOM `base_raw` 数据动态映射灰度图。
    *   **滤镜图层 (`_update_bg_cache`)**: 【核心性能优化】采用缓存机制 `_raw_filtered`。只有在改变滤镜类型或强度时，才调用 OpenCV 或 SimpleITK 进行医学级增强（CLAHE, 锐化, 平滑）。
    *   **调窗处理**: 将 16 位的缓存数据快速线性映射到 8 位显示范围（Window Center / Window Width），保证拖拽滑块时的绝对丝滑。
    *   **掩码图层**: 使用 `QImage.Format_Indexed8` 和颜色表高效渲染带透明度的掩码。
*   **交互式视口控制:**
    *   `fit_view()`: 自动计算缩放比例，使图像适应当前窗口大小。
    *   右键拖拽：实现画面的平移（Pan）。
    *   `Ctrl + 滚轮`：以鼠标光标为中心进行无级缩放。
*   **标注工具 (ToolType):**
    *   **画笔/橡皮**: 纯滚轮调整画笔大小，支持连续涂抹插值（防止鼠标移动过快导致断点）。
    *   **多边形**: 点击打点，空格键闭合，使用 `cv2.fillPoly` 填充区域。
*   **智能辅助功能:**
    *   `_snap_pos()`: 边缘吸附。利用 Sobel 算子计算梯度，将画笔中心自动吸附到高对比度的边缘上。
    *   `smart_lock_enabled`: 亮度锁定。涂抹时只对指定阈值以上（亮部/骨骼）或以下（暗部/气体）的区域生效，保护复杂边界。
*   **撤销重做机制:**
    *   维护 `undo_stack` 和 `redo_stack`，在每次绘制开始时保存 `mask` 的深拷贝，实现 Ctrl+Z 和 Ctrl+Y。