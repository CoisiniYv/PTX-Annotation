# data_mixin.py 说明文档

## 功能概述
`data_mixin.py` 是应用程序的数据驱动引擎。它负责文件系统的扫描、病例的分类、图像的高性能内存缓存（LRU）以及多线程的后台数据预取（Prefetching）。

## 主要逻辑与功能

*   **异步扫描与识别 (`refresh_lists`)**:
    *   启动一个后台线程遍历选定的“原图目录”。
    *   **CT 模式**：将一级子目录视为一个病例；**X光模式**：将每个图片文件视为一个病例。
    *   自动比对“掩码目录”或 `case_status.json`，判定病例是“已标注 (Completed)” 还是 “待标注 (Todo)”，并推送到对应的 UI 列表中。
*   **状态与标签持久化**:
    *   自动加载和保存 `pneumothorax_labels.json`（气胸标签）和 `case_status.json`（完成状态）。
    *   支持任务 JSON 模式（Task Mode），根据下发的 JSON 列表限定工作范围。
*   **高性能内存与对象池 (`_image_cache` & `_mask_pool`)**:
    *   实现了一个 LRU（最近最少使用）缓存策略，防止内存溢出。
    *   实现了一个简单的掩码内存池 `_alloc_mask`，复用 numpy 数组，极大降低加载高分辨率图片时的内存分配开销和 GC 停顿。
*   **多线程异步预取 (Prefetching)**:
    *   **序列内预取 (`_start_prefetch`)**: 当用户在看第 $N$ 张图时，后台线程自动加载 $N-1$ 和 $N+1$ 等附近的切片。
    *   **跨病例预取 (`_start_cross_case_prefetch`)**: 提前读取列表中下一个病例的数据，保证医生在切换病人时做到“零等待”。
    *   使用 `epoch` 和 `token` 机制确保在用户快速切换时，能安全中止旧的预取任务，防止脏数据污染。
*   **异步缩略图生成 (`_thumb_worker`)**:
    *   CT 模式下，序列缩略图由后台线程逐帧生成并压入队列。
    *   主线程通过定时器 `_drain_thumb_queue` 批量消费并更新 UI，保证加载含有几百张切片的 CT 序列时主界面绝不卡死。